# Changelog

## v1.1.10

### 优化

- 小红书原图超过 10MB 时自动压缩为 JPEG，降低发送失败概率。

## v1.1.9

### 浼樺寲

- NGA 和小红书截图统一经过 JPEG 压缩，即使原图不超过 5MB 也会压缩一次；超限时继续自动降低质量或尺寸。

## v1.1.8

### 修复

- 修复微博读取标准 `cookies.txt` 文件时，已解析的 Netscape Cookie 又被当作多行 `k=v;...` 文本重复解析，导致 HTTP Cookie header 非法的问题。

## v1.1.7

### 修复

- 微博 Cookie 支持保存到并自动读取 `cookies/weibo_cookies.txt`，兼容 `cookies.txt` 与 `k=v;...` 两种格式。
- 微博详情接口返回登录态失效、`data/status/mblog` 包裹结构或仅含 `idstr/bid/mid` 时，给出更准确的解析结果或错误提示。
- NGA 主楼和热点回复会拆分为多张截图发送，降低单张截图过高或过大导致发送失败的概率。

## v1.1.6

### 优化

- B 站视频满足下载发送条件时，会在发送视频前先发送视频标题、简介和链接文本。

## v1.1.5

### 调整

- 除 B 站和 NGA 外，抖音、小红书、微博、X/Twitter 在包含视频时会将视频作为普通消息单独发送，合并转发聊天记录中仅保留渲染卡片、文本摘要、评论截图和图片，减少历史视频过期后的影响。

## v1.1.4

### 修复

- NGA 帖子截图会限制高度不超过 10000px，并在超过 5MB 时自动压缩，降低图片过长或过大导致发送失败的概率。
- 微博长文会在详情接口缺少完整正文时额外补全长文内容，减少正文被“展开全文”截断的问题。

## v1.1.3

### 修复

- 优化 NGA 帖子截图等待图片加载的逻辑，减少懒加载图片未出现在截图里的情况。
- 延长 NGA 截图任务超时时间，给图片较多的帖子更多渲染时间。
- 小红书评论截图不再主动展开楼中楼回复，避免楼中楼展开后产生过长空白。
- 小红书评论截图会尝试移除底部固定互动栏，减少截图内容遮挡。

## v1.1.2

### 修复

- 小红书评论截图改用插件数据目录托管 Playwright Chromium，并在首次使用时自动尝试安装浏览器。
- 小红书评论截图增加上下空白裁剪，减少楼中楼展开后截图出现大段空白的问题。
- NGA 帖子截图复用统一 Chromium 启动逻辑，降低 Docker 环境缺少默认浏览器缓存时的失败率。

## v1.1.1

### 修复

- 修复小红书评论截图丢失 `xsec_token` 等访问参数导致部分笔记跳转 404 的问题。
- 优化小红书无 Cookie 场景下登录弹窗遮挡处理，截图前会尝试关闭或移除登录弹层。
- 优化小红书楼中楼展开逻辑，兼容 `展开 n 条回复` 文案中的空格。

## v1.1.0

### 新增

- 新增 NGA 论坛链接解析，支持 `bbs.nga.cn`、`ngabbs.com`、`nga.178.com` 等帖子链接。
- NGA 解析使用网页截图发送帖子内容，并支持下载主楼/热点区域的附件图片追加到聊天记录。
- 新增 `nga_settings.cookies`、`nga_settings.merge_send`、`nga_settings.max_attachment_images` 配置项。
- NGA Cookie 支持 `cookies.txt` 与 `k=v;...` 两种格式，并自动在 `ngabbs.com` / `nga.cn` / `nga.178.com` 常用域名间共用。

### 变更

- 插件版本号更新到 `1.1.0`。
- B站超长视频提示中补充原视频链接，方便手动打开观看。

## v1.0.10

### 新增

- 新增微博图文和视频解析，支持原图优先、访客 Cookie 和自定义 Cookie
- 新增 X/Twitter 图文和视频解析，支持 `twitter.com` 与 `x.com`
- 新增群黑名单/白名单配置，私聊始终放行
- 新增多表情回应，可选择随机一个或按顺序依次回应；显式空列表表示不回应
- B站、抖音和小红书新增文字摘要模式，可按需切换回渲染卡片
- 新增自定义字体与托管字体安装，字体优先级为：用户配置 > 插件托管字体 > 系统字体

### 优化

- 扩展中文字体查找范围，并增加 `fc-match` 兜底
- 托管字体下载移到后台线程，避免阻塞 AstrBot 事件循环
- 插件配置、Cookie、字体和缓存统一保存在 AstrBot 插件数据目录
- 微博正文优先展开长文，图片优先原图/大图，视频优先最高码率
- JSON 卡片入口与文本链接入口统一应用平台开关和群过滤

### 修复

- 修复 B站部分画质枚举值为空时的崩溃
- 兼容 B站 `hvc1` / `hev1` HEVC 编码标签
- 修复跨机器部署时合并转发视频使用本地 `file://` 路径导致 NapCat `ENOENT`
- 修复微博和 X 发送失败时未清理临时媒体文件
- 修复小红书入口层把协程当作异步生成器迭代，导致 `async for requires __aiter__` 的问题
- 修复小红书卡片渲染配置刷新后未及时生效

## v1.0.9

### 变更

- 配置面板改为分组折叠结构，配置项迁移为按平台分组
- 小红书移除 Cookies 支持，优化图片下载逻辑（CDN 原图 / CI-PNG 转码）
- 新增「小红书图片并发下载」配置项
- 新增「小红书优先 CI-PNG 转码」配置项
- 新增「小红书图片转文件阈值(MB)」配置项，超限图片自动转为文件发送
- 调整抖音 / 小红书视频合并发送默认值为关闭

### 修复

- 修复小红书视频下载链路参数缺失导致的异常
- 修复小红书解析超大图时 pipeline 错误

### ⚠️ 升级须知

- v1.0.8 及更早版本升级时，配置项会按新结构重建，建议事先复制 B 站 Cookies，升级后重新粘贴

## v1.0.8

### 修复

- 修复 v1.0.6 修改 UA 导致抖音解析失败的问题

## v1.0.7

### 新增

- 新增 `error_notify_mode` 配置项，控制解析/下载失败时 Bot 对用户的反馈行为：
  - **静默**（默认）：仅记录日志，不向群内发送任何错误消息
  - **脱敏**：发送通用错误提示（如"视频下载失败"），不包含异常详情
  - **报错**：发送完整错误信息，包含异常详情（旧版默认行为）

### 修复

- 修复 B 站视频下载/发送失败时将 `str(exc)` 直接发送给用户，可能泄露内部信息（文件路径、网络错误详情、API 响应等）的问题

## v1.0.6

### 新增

- 新增 `merge_send_as_sender` 配置项，合并转发的发送人可显示为发送链接的用户

### 优化

- ~~引入 `CHROME_UA` 常量，统一各平台 User-Agent~~（已在 v1.0.8 回退）
- 解析任务管理从全局变量迁移为实例级任务池，插件重载时自动清理旧任务
- 统一使用 `time.perf_counter()` 记录耗时
- 优化小红书图片下载逻辑
- 优化日志记录格式

### 移除

- 移除 API 超时时间配置项（无实际作用）
- 移除自动清理延迟配置项（无实际作用）

## v1.0.5

### 修复

- 修复 B 站并发发送时 ENOENT 竞态：下载文件增加 `request_id` 后缀避免同名冲突，发送前增加文件存在性校验

## v1.0.4

### 新增

- 新增 `bili_max_duration_seconds` 配置项，支持 B 站视频最大时长限制
- 增强小红书重试机制与错误处理

## v1.0.3

### 修复

- 修复短链接解析异常处理并优化图片下载逻辑

## v1.0.2

### 优化

- 重构路径管理，改善 Cookie 处理逻辑

## v1.0.1

### 新增

- 初始版本：支持 B 站 / 抖音 / 小红书链接自动解析
- 插件配置页面支持粘贴 B 站 cookies.txt
